{
  "name": "taxirecibo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:client": "npm run dev --workspace=client",
    "dev:server": "npm run dev --workspace=server",
    "build": "npm run build --workspace=client && node_modules/.bin/prisma generate --schema=server/prisma/schema.prisma && npm run build --workspace=server && mkdir -p server/dist/public && cp -r client/dist/. server/dist/public/",
    "start": "npx prisma migrate deploy --schema=server/prisma/schema.prisma && node server/dist/index.js",
    "seed": "npm run seed --workspace=server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
